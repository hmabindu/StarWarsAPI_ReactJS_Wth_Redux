# StarWarsAPI_ReactWithRedux
First, install the dependencies.

```
npm install
```

To build and serve the project locally at localhost:6060, run:

```
npm start
```

To just build the project, run:

```
npm run build
```
